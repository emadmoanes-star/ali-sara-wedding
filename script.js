const intro=document.getElementById('intro');const envelope=document.getElementById('envelope');const openBtn=document.getElementById('openInvitation');const music=document.getElementById('weddingMusic');const musicBtn=document.getElementById('musicToggle');const musicCardButton=document.getElementById('musicCardButton');const musicStatus=document.getElementById('musicStatus');
openBtn.addEventListener('click',()=>{envelope.classList.add('open');setTimeout(()=>{intro.classList.add('hidden');window.scrollTo({top:0,behavior:'smooth'});},900);});
function toggleMusic(){if(music.paused){music.play().then(()=>{musicBtn.textContent='❚❚';musicCardButton.textContent='Pause Music';musicStatus.textContent='Playing wedding music.'}).catch(()=>{musicStatus.textContent='Add assets/wedding.mp3 to enable music.'})}else{music.pause();musicBtn.textContent='♫';musicCardButton.textContent='Play Music';musicStatus.textContent='Music paused.'}}
musicBtn.addEventListener('click',toggleMusic);musicCardButton.addEventListener('click',toggleMusic);
const weddingDate=new Date('2026-09-02T20:00:00+03:00').getTime();function tick(){const d=weddingDate-Date.now();if(d<=0){['days','hours','minutes','seconds'].forEach(id=>document.getElementById(id).textContent='00');return}const day=86400000,hour=3600000,minute=60000;document.getElementById('days').textContent=String(Math.floor(d/day)).padStart(2,'0');document.getElementById('hours').textContent=String(Math.floor((d%day)/hour)).padStart(2,'0');document.getElementById('minutes').textContent=String(Math.floor((d%hour)/minute)).padStart(2,'0');document.getElementById('seconds').textContent=String(Math.floor((d%minute)/1000)).padStart(2,'0')}tick();setInterval(tick,1000);
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
// When the couple's licensed audio is uploaded, set the exact chorus start time here if desired, e.g. music.currentTime=42;
music.addEventListener('error',()=>{musicStatus.textContent='Add your legally obtained song file at assets/wedding.mp3.'});